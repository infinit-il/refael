chartPath: demo/guestbook
values: |
  guestbook:
    containers:
      server:
        dev:
          enabled: false
          # enabled: true
    ingress:
      enabled: true

valueFiles: "$environments/dev/component.yaml, $environments/dev/re.yaml"
