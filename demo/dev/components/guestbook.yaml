chartPath: demo/guestbook
values: |
  guestbook:
    containers:
      server:
        dev:
          enabled: false
          # enabled: true
    ingress:
      enabled: true

valueFiles: [ "helm/values/common.yaml", "helm/values/staging.yaml" ]