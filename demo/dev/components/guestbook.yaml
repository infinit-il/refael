chartPath: demo/guestbook
values: |
  guestbook:
    containers:
      server:
        dev:
          enabled: false
          # enabled: true
    ingress:
      enabled: true


# valueFile1: $environments/dev/tst-demo.yaml

